Feature:

Background: This executes once per feature file
	Given these properties
		|portal.release|true|
		|portal.upstream|true|
		|test.run.environment|EE|
		|testray.main.component.name|Auditing|
		|testray.testcase.product.edition|EE|

Setup: This executes before each scenario 
	Given TestCase set Up Portal Instance
	When User first Login PG

Teardown: This executes after each scenario 
	Given PortalInstances tear Down CP
		|testPortalInstance|TestPropsUtil#get('test.portal.instance')|
Scenario: View Resource Actions CP
Priority: 4
	Given these properties
		|test.name.skip.portal.instance|CPAuditreportsUsecaseEE#ViewResourceActionsCP|
	When ProductMenu goto Control Panel Configuration
		|portlet|Server Administration|
	And ServerAdministration execute Server Resources Actions
		|actionsDescription|Reindex all search indexes.|
	And TestCase get Site Name
##########################################################
The Poshi return element is not supported in the readable syntax. Please update this test.
##########################################################
		|siteName|${siteName}|
	And ProductMenu goto Control Panel Users
		|portlet|Users and Organizations|
	And these variables
		|key_userScreenName|test|
	And AssertClick the value 'test' at locator 'UsersAndOrganizations#USER_TABLE_SCREEN_NAME'
	And Pause at locator '5000'
	And these variables
		|userId||
	And ProductMenu goto Control Panel Configuration
		|portlet|Audit|
	And Audit search CP
		|searchTerm|${userId}|
		|userId|${userId}|
		|userName|Test Test|
	And ProductMenu goto Control Panel Configuration
		|portlet|Audit|
	And Audit search CP
		|searchTerm|Test Test|
		|userName|Test Test|
	And ProductMenu goto Control Panel Configuration
		|portlet|Audit|
	And Audit search CP
		|resourceId|${userId}|
		|searchTerm|${userId}|
		|userName|Test Test|
	And ProductMenu goto Control Panel Configuration
		|portlet|Audit|
	And Audit search CP
		|resourceName|User|
		|searchTerm|User|
		|userName|Test Test|
	And ProductMenu goto Control Panel Configuration
		|portlet|Audit|
	And Audit search CP
		|resourceAction|Login|
		|searchTerm|Login|
		|userName|Test Test|
	And User logout PG
	And User login User Invalid PG
		|password|user|
		|userEmailAddress|test@liferay.com|
	And User login User PG
		|password|test|
		|userEmailAddress|test@liferay.com|
	And ProductMenu goto Control Panel Configuration
		|portlet|Audit|
	And these variables
		|key_resourceAction|Login Failure|
	And these variables
		|key_resourceName|User|
	And AssertTextEquals the value 'Login Failure' at locator 'AuditReports#REPORTS_TABLE_RESOURCE_ACTION'
	And ProductMenu goto Control Panel Users
		|portlet|Users and Organizations|
	And User add CP
		|userEmailAddress|test1@liferay.com|
		|userFirstName|test1|
		|userLastName|test1|
		|userScreenName|test1|
	And ProductMenu goto Control Panel Users
		|portlet|Users and Organizations|
	And User edit Password CP
		|userEmailAddress|test1@liferay.com|
		|userScreenName|test1|
	And ProductMenu goto Control Panel Users
		|portlet|Users and Organizations|
	And User impersonate CP
		|siteName|${siteName}|
		|userScreenName|test1|
	And Close close Window at locator 'title=Welcome - ${siteName}'
	And SelectWindow at locator 'title=Users and Organizations - Control Panel - ${siteName}'
	And ProductMenu goto Control Panel Configuration
		|portlet|Audit|
	And these variables
		|key_resourceAction|Impersonate|
	And these variables
		|key_resourceName|User|
	And AssertTextEquals the value 'Impersonate' at locator 'AuditReports#REPORTS_TABLE_RESOURCE_ACTION'
	And ProductMenu goto Control Panel Users
		|portlet|Roles|
	And Role add
		|roleTitle|Role1|
	And ProductMenu goto Control Panel Users
		|portlet|Roles|
	And Role assign Reg Role CP
		|roleTitle|Role1|
		|userScreenName|test1|
	And ProductMenu goto Control Panel Users
		|portlet|User Groups|
	And UserGroup add CP
		|userGroupName|User Group1|
	And UserGroup assign Member CP
		|userGroupName|User Group1|
		|userScreenName|test1|
	And ProductMenu goto Control Panel Configuration
		|portlet|Audit|
	And these variables
		|key_resourceAction|Add|
	And these variables
		|key_resourceName|Role|
	And AssertTextEquals the value 'Role' at locator 'AuditReports#REPORTS_TABLE_RESOURCE_NAME'
	And these variables
		|key_resourceAction|Add|
	And these variables
		|key_resourceName|Role|
	And AssertTextEquals the value 'Add' at locator 'AuditReports#REPORTS_TABLE_RESOURCE_ACTION'
	And these variables
		|key_resourceAction|Assign|
	And these variables
		|key_resourceName|User|
	And AssertTextEquals the value 'User' at locator 'AuditReports#REPORTS_TABLE_RESOURCE_NAME'
	And these variables
		|key_resourceAction|Assign|
	And these variables
		|key_resourceName|User|
	And AssertTextEquals the value 'Assign' at locator 'AuditReports#REPORTS_TABLE_RESOURCE_ACTION'
	And these variables
		|key_resourceAction|Add|
	And these variables
		|key_resourceName|User Group|
	And AssertTextEquals the value 'User Group' at locator 'AuditReports#REPORTS_TABLE_RESOURCE_NAME'
	And these variables
		|key_resourceAction|Add|
	And these variables
		|key_resourceName|User Group|
	And AssertTextEquals the value 'Add' at locator 'AuditReports#REPORTS_TABLE_RESOURCE_ACTION'
	And AssertTextEquals the value 'User' at locator 'xpath=(//tr[contains(.,'Assign') and contains(.,'User')]/td[4])[2]'
	Then AssertTextEquals the value 'Assign' at locator 'xpath=(//tr[contains(.,'Assign') and contains(.,'User')]/td[5])[2]'