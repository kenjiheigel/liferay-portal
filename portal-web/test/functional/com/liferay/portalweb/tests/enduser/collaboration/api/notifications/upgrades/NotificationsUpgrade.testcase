@component-name = "portal-upgrades"
definition {
	property ignore.errors = "Plugin security management is not enabled. Enable a security manager, then restart.";
	property portal.release = "true";
	property portal.upstream = "true";
	property testray.main.component.name = "Upgrades Collaboration";

	setUp {
		SignIn.signIn(
			userEmailAddress = "user@liferay.com"
		);

		Smoke.viewWelcomePage();

		ProductMenu.gotoPortlet(
			category = "Configuration",
			panel = "Control Panel",
			portlet = "Search"
		);

		SearchAdministration.executeSearchActions(
			actionsDescription = "Reindex all search indexes."
		);
	}

	@priority = "5"
	testViewNotifications621015 {
		property data.archive.type = "data-archive-notifications";
		property portal.version = "6.2.10.15";

		Upgrade.viewNotifications();
	}

	@priority = "5"
	testViewNotifications623 {
		property data.archive.type = "data-archive-notifications";
		property portal.version = "6.2.3";

		Upgrade.viewNotifications();
	}

	@priority = "5"
	testViewNotifications625 {
		property data.archive.type = "data-archive-notifications";
		property portal.version = "6.2.5";

		Upgrade.viewNotifications();
	}
}