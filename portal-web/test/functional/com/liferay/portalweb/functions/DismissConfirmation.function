@default = "dismissConfirmation"
@summary = "Execute the dismiss of a confirmation window"
definition {

	function dismissConfirmation {
		selenium.dismissAlert();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();

		WaitForSPARefresh();
	}

}