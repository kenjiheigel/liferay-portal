@default = "isChecked"
definition {
	function isChecked {
		WaitForSPARefresh();

		selenium.waitForElementPresent();

		selenium.waitForVisible();

		selenium.isChecked();
	}

	function isCheckedNotVisible {
		WaitForSPARefresh();

		selenium.waitForElementPresent();

		selenium.isChecked();
	}
}