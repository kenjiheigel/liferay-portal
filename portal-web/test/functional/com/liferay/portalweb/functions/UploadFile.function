@default = "uploadFile"
definition {
	function uploadFile {
		WaitForSPARefresh();

		selenium.waitForVisible();

		selenium.uploadFile();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}
}