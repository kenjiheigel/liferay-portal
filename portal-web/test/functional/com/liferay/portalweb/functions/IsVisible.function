@default = "isVisible"
definition {
	function isVisible {
		WaitForSPARefresh();

		selenium.isVisible();
	}

	function waitForVisible {
		WaitForSPARefresh();

		selenium.waitForVisible();

		IsVisible.isVisible();
	}
}