@default = "select"
definition {
	function pauseSelect {
		WaitForSPARefresh();

		selenium.pause("5000");

		SelectNoError.select();
	}

	function select {
		WaitForSPARefresh();

		selenium.waitForVisible();

		selenium.mouseOver();

		selenium.select();
	}
}