@default = "sikuliAssertElementPresent"
definition {
	function sikuliAssertElementPresent {
		WaitForSPARefresh();

		selenium.fail("Forced sikuli failure");

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}
}