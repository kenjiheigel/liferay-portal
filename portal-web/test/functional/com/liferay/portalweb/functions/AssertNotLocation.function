@default = "assertNotLocation"
definition {
	function assertNotLocation {
		WaitForSPARefresh();

		selenium.assertNotLocation();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}
}