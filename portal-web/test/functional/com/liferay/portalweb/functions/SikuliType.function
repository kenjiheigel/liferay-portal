@default = "sikuliType"
definition {
	function sikuliType {
		WaitForSPARefresh();

		selenium.fail("Forced sikuli failure");

		selenium.pause("2000");

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}
}