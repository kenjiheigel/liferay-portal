@default = "sikuliType"
definition {
	function sikuliType {
		WaitForSPARefresh();

		selenium.fail();

		selenium.pause("2000");

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}
}